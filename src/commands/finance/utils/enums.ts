export enum FINANCE_INPUT_ACTIONS {
  FINANCE = "FINANCE",
}

export enum FINANCE_ACTIONS_TYPES {
  PUT = "PUT",
  TAKE = "TAKE",
}

export enum FINANCE_BOXES_ENUM {
  ACCUM = "ACCUM",
  POCKET = "POCKET",
}
