export enum CostTimeEnum {
  DAY,
  MONTH,
  SEASON,
  YEAR,
}

export enum CurrencyEnum {
  RUB = "rub",
  EUR = "eur",
  USD = "usd",
}

export enum LastPlacesEnum {
  FINANCE = "FINANCE",
  COST = "COST",
  STATISTICS = "STATISTICS",
}
